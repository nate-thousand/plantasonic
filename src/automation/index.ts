/**
 * Automation hook interface (Phase 8 — no implementation).
 */

export type { AutomationProvider, AutomationRegistry } from './types.ts';
export { automationRegistry, NullAutomationRegistry } from './types.ts';
